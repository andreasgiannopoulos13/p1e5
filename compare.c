#include <studio.h>

int main (void)

{
	double c,d;
	 
	 printf ("enter numbers:");
	 scanf ("%1f",&c);
	 
	 printf("enter numbers:");
	 scanf ("%1f",&d);
	 
	 if (c<d){
	 	printf("o mikroteros arithmos einai = %1f,\n",c);
	 }
	 
	 if (c>d){
	 	printf("o mikroteros arithmos einai = %1f,\n",d);
	 }
	 
	 return 0;
}
