#include <studio.h>

int main (void)
{
	double a,b;
	printf("dose dyo arnitikous arithmoys");
	scanf ("%1f %1f", &a , &b);
	if (a, b < 0 ) {
		printf("sfalma");
		
	}
	printf ("%1f %1f", -a ,-b );
	return 0;
}

